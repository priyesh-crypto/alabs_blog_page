export const metadata = {
  title: "Beyond Transformers: The Rise of Linear Recurrent Units | AnalytixLabs",
  description:
    "Exploring how Linear Recurrent Units offer a paradigm shift in sequence modeling with linear scaling and O(1) state updates during inference.",
};

export default function ArticleLayout({ children }) {
  return children;
}
